/**
 * The function returns a SignUp component from the "@clerk/nextjs" library in a TypeScript React
 * application.
 */
import { SignUp } from "@clerk/nextjs";
 
export default function Page() {
  return <SignUp />;
}
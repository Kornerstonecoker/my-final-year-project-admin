/**
 * The function returns a SignIn component from the "@clerk/nextjs" library in a TypeScript React
 * application.
 */
import { SignIn } from "@clerk/nextjs";
 
export default function Page() {
  return <SignIn />;
}
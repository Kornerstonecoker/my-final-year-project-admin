/**
 * The ToasterProvider function returns a React component that renders a Toaster component.
 */
"use client"
import { Toaster } from "react-hot-toast"

export const ToasterProvider = () => {
  return <Toaster/>
}